import { describe, it, expect, beforeEach } from 'vitest';
import { GameEngine } from './GameEngine';
import { GameConfig } from './GameConfig';
import { AIController } from './AIController';

describe('AIController Logic', () => {
    let engine: GameEngine;
    let ai: AIController;

    beforeEach(() => {
        engine = new GameEngine();
        (globalThis as any).AI_LOGS = [];
        // Setup P1 as AI
        engine.state.players['P1'].isAI = true;
        engine.state.currentPlayerId = 'P1';
        ai = engine.ai;

        // Clean grid
        for (let r = 0; r < GameConfig.GRID_HEIGHT; r++) {
            for (let c = 0; c < GameConfig.GRID_WIDTH; r++) { // WAIT! IS THIS "r++" meant to be "c++"?
                // OH MY GOD! I found a TYPO!
            }
        }
    });
});
